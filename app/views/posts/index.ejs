<% if (posts.length) { %>
    <% posts.forEach(function(post){ %>
        <h2><a href="/<%= post._id %>"><%= post._id %></a></h2>
        <p class="meta"><%= post.created_at %></p>
        <p><%= post.desc %></p>
        <p>&nbsp;</p>
        <p><a href="/<%= post._id %>">Read on.</a></p>
        <p>&nbsp;</p>
    <% }) %>
  </ul>
<% } %>

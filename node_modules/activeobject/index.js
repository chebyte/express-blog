/*var attributeMethods = require("/attributeMethods");
var hasOne = require("/hasOne");
var hasMany = require("/hasMany");

module.exports = function(props) {
  
  attributeMethods(props);
  hasOne(props);
  hasMany(props);
  
  function Clazz() {
    
  }
  
  Model.prototype = props;
  
  return Clazz;
  
}*/

module.exports = require(__dirname + "/lib/activeobject.js");